@model Tuple<IEnumerable<WebCode01.ViewModels.ProjectFileListViewModel>,int>
@{
    ViewBag.Title = "Project Files";
}

<h2>Project Files</h2>


<div class="dropdown">
    <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">
        File Type
        <span class="caret"></span>
    </button>
    <ul class="dropdown-menu">
        <li>@Html.ActionLink("All Files", "Index", new {projectId = Model.Item2})</li>
        <li>@Html.ActionLink("C#", "FilesByType", new { projectId = Model.Item2, fileType = "C#" })</li>
        <li>@Html.ActionLink("C++", "FilesByType", new { projectId = Model.Item2, fileType = "C++" })</li>
        <li>@Html.ActionLink("Javascript", "FilesByType", new { projectId = Model.Item2, fileType = "Javascript" })</li>
        <li>@Html.ActionLink("HTML", "FilesByType", new { projectId = Model.Item2, fileType = "HTML" })</li>
        <li>@Html.ActionLink("CSS", "FilesByType", new { projectId = Model.Item2, fileType = "CSS" })</li>
    </ul>
</div>

<div class="container">
    <div class="row">
        <div>
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>File Name</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach(var item in Model.Item1)
                    {
                        <tr>
                            <td>@Html.ActionLink(item.fileName, "Editor", new { fileId = item.id})</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>         
    </div>
</div>

<div class="container">
    <span class="row">
        @Html.ActionLink("Upload File", "AddFile", "ProjectFiles", new { projectId = Model.Item2 }, new { @class="btn btn-default"})
        @Html.ActionLink("Current Members", "ProjectMembers", "ProjectFiles", new { projectId = Model.Item2 }, new { @class = "btn btn-default" })
        @Html.ActionLink("Add Member", "AddMember", new { projectId = Model.Item2 }, new { @class = "btn btn-default" })
        @Html.ActionLink("Add Blank File", "CreateBlankFile", new { projectId = Model.Item2 }, new { @class = "btn btn-default" })
    </span>
    <div>
        @Html.ActionLink("Back to Project List", "Index", "Project", null, new { @class = "btn btn-primary"})
    </div>
</div>